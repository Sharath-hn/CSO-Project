_schema-version: "3.2"
ID: combrsr
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: combrsr-destination-content
  type: com.sap.application.content
  requires:
  - name: combrsr-destination-service
    parameters:
      content-target: true
  - name: combrsr-repo-host
    parameters:
      service-key:
        name: combrsr-repo-host-key
  - name: combrsr-uaa
    parameters:
      service-key:
        name: combrsr-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: combrsr_html_repo_host
          ServiceInstanceName: combrsr-html5-srv
          ServiceKeyName: combrsr-repo-host-key
          sap.cloud.service: combrsr
        - Authentication: OAuth2UserTokenExchange
          Name: combrsr_uaa
          ServiceInstanceName: combrsr-xsuaa-srv
          ServiceKeyName: combrsr-uaa-key
          sap.cloud.service: combrsr
        existing_destinations_policy: ignore
  build-parameters:
    no-source: true
- name: combrsr-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: combrsr-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - combrsr.zip
      name: combrsr
      target-path: resources/
- name: combrsr
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: combrsr-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: combrsr-destination-service
    service-plan: lite
- name: combrsr-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: combrsr-xsuaa-srv
    service-plan: application
- name: combrsr-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: combrsr-html5-srv
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
